apiVersion: apps/v1
kind: Deployment
metadata:
  name: cgm-deployment
spec:
  selector:
    matchLabels:
      app: cgm
  template:
    metadata:
      labels:
        azure.workload.identity/use: "true"
        app: cgm
    spec:
      serviceAccountName: cgm-sa
      containers:
        - name: cgm
          image: ghcr.io/happymary16/cgm/cgm:latest
          args:
            - cgm-service
          ports:
            - containerPort: 8080
      imagePullSecrets:
        - name: github-secret
